import 'package:flutter/material.dart';

class Taksho extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Taksho'),
      ),
    );
  }
}
